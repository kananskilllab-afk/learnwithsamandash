# Leave empty for local dev — Vite proxies /api to the server (see vite.config.js).
# In production, point this at your deployed API's origin, e.g. https://api.learnwithsamandash.com
VITE_API_BASE_URL=
